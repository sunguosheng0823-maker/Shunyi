# 版本记录

## 0.1.0 — 2026-09-15（预发布）

首次发布免账号远程终端版本。

### 功能

- Mac 项目目录树、常见编程语言及 Markdown 只读预览、按项目保留的本地终端。
- 独立远程主机列表、筛选与显示选择、顶部终端标签和右侧快捷指令库。
- 长期连接证书、一次性临时密码、手动和周期证书轮换、临时密码生成和作废。
- Mac 本机共享及凭据管理，独立 Agent CLI，macOS/Linux 后台服务模板。
- 自托管中继、设备挑战签名注册、端到端 TLS 1.3、有界转发和 WSS 支持。
- SSH 使用系统 `known_hosts`，未知或变化的服务器密钥被拒绝。

### 兼容性与使用规则

- UniRC-T v2 需要客户端、Agent 和中继配套更新，拒绝旧版未认证连接流程。
- 一次性密码在首次认证成功时消耗，授权可包含多个终端，断开后不能重用。
- 更新凭据影响后续连接，已有授权继续工作。需要结束当前访问时停止共享。
- 本机共享默认关闭，root 终端需要管理员在设备端明确配置服务运行用户。

### 发布前验证

- 本机后端测试 14 项通过，包含真实中继、Agent、TLS 与 PTY 集成测试。
- Mac 本地 PTY 工作目录测试 1 项通过；后端 Clippy 检查通过。
- 原生 Mac 完成证书选择、设备 ID 填写和真实终端连接；安装构建完成项目、终端输出与快捷指令验证。
- 940×600 弹窗、错误提示、修改认证信息后重连及临时密码不持久化已验证。

### 当前限制

下载包面向 macOS Apple Silicon，使用 ad-hoc 开发签名，尚未进行 Apple Developer ID 签名和公证。首次本机验收没有覆盖公网多机、Linux/Windows 实机、长期压力测试或独立安全审计；CI 的实际结果以平台运行记录为准。

账号登录、同账号设备发现、P2P、远程桌面、会话录像及终端任务断线保活不在本版中。
