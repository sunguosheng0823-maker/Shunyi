# 当前加密实现

2026-09-15 的免账号版本使用端到端 TLS 1.3，由 `rc-access` 统一实现。设备 CA 固定设备身份，长期连接使用客户端证书，临时密码只在验证设备身份后的加密通道里使用。

此前自定义 X25519/AES-GCM 试验代码已从运行接口中移除。原来的共享 Token + 明文 SessionOpen 不能创建终端，不提供关闭端到端认证的开关。

详细机制见 [安全边界](SECURITY.md) 和 [UniRC-T v2](protocol/UniRC-T-v2.md)。外层 WSS 的证书用于验证中继，内层设备证书用于验证被控设备，两者用途不同。
