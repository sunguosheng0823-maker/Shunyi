# 瞬移设备访问

瞬移免账号开源版以设备证书或单次临时密码授权远程终端；账号业务明确延期。

## Language

**被控设备**：运行 Agent 并接受认证后远程终端连接的一台机器。凭据归属于设备，不归属于终端标签。

**设备 ID**：由设备 CA 证书摘要构成的固定标识，用于定位设备并校验其 TLS 身份。本身不授予访问权限。

**连接证书**：长期访问凭据，由设备端软件生成并导出，包含客户端证书、私钥与设备公开身份。只有当前证书能够建立新访问。

**临时密码**：软件生成的高熵一次性凭据，首次认证成功后持久化消耗。只有一个待使用密码，断开后不能重连。

**凭据轮换**：替换长期连接证书或重新生成待使用临时密码。旧凭据不能建立新访问，已授权访问默认继续；停止共享才会结束当前访问。

**设备访问会话**：一条经过认证的端到端 TLS 设备连接，可拥有多个终端会话。最后一个终端关闭或连接断开后结束。

**终端会话**：设备访问中的一个 PTY/Shell，不持有独立密码。

**终端身份**：执行命令的操作系统用户。证书认证不等于提权；root 终端需由设备管理员明确运行 root Agent。

**中继准入令牌**：可选的中继连接门槛，不是设备凭据或账号身份。

**设备在线状态**：设备与中继当前的可达性，不代表账号归属。图形界面的“共享服务运行中”只表示 Agent 进程运行，不宣称公网已可达。

**瞬移账号 / 同账号连接**：未来独立业务，本次不开发，也不启用跨端设备清单。
