UNIRC_BIND=127.0.0.1:8080
# Optional; both agent and clients must use it when enabled.
# UNIRC_TOKEN=replace-with-a-relay-admission-token
# For built-in WSS instead of a reverse proxy, provide both:
# UNIRC_TLS_CERT=/etc/shunyi/tls/fullchain.pem
# UNIRC_TLS_KEY=/etc/shunyi/tls/privkey.pem
