UNIRC_SERVER=wss://relay.example.com
UNIRC_STATE_DIR=/var/lib/shunyi-agent
UNIRC_DEVICE_NAME=my-server
UNIRC_SHELL=/bin/bash
# Optional infrastructure admission token; not a device-access credential.
# UNIRC_TOKEN=replace-with-a-relay-admission-token
