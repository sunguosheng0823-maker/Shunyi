# 安全边界

## 免账号设备访问

- 每台设备拥有本地生成的 P-256 CA 身份。设备 ID 为 `rc-` 加 CA DER 证书的 SHA-256 十六进制摘要。设备 ID 是公开的定位与身份校验信息，本身不授予访问权限。
- Agent 注册时签署中继新生成的挑战和设备名称。中继验证签名、设备 ID 和连接实例，拒绝同一设备的并发身份替换。旧连接退出不能删除另一实例。
- UniRC-T v2 把 TLS 1.3 字节流放入有界 WebSocket 隧道。两端执行标准 X.509 验证。中继不能解密临时密码、终端输入或输出；中继仍能观察设备 ID、在线时间、数据量和连接时序，并能拒绝服务。
- 长期访问使用 CA 签发的客户端证书，设备还校验客户端证书是否为当前有效版本。轮换会撤销旧证书的新连接权限，不自动打断已经授权的终端。
- 临时访问在 TLS 验证设备身份后发送密码。密码由 CSPRNG 生成（192 位随机量），只存带随机盐和域分隔的 SHA-256 校验值，用常量时间比较。这个方案仅用于软件生成的高熵密码，不提供低熵自定义临时密码。
- 临时密码只有一次成功认证机会，持久化标记在认证确认和 PTY 创建之前写入。并发请求最多一方成功。错误密码和 TLS 握手失败不消耗未使用密码；在正确密码已被设备确认后，即使客户端没有收到响应，也需要重新生成，避免不确定状态重用。
- 临时授权仅属于该条 TLS 设备访问连接，可以包含多个 PTY。最后一个 PTY 关闭、应用退出、网络断开或心跳超时结束授权；不会用旧临时密码自动重连。没有 PTY 的已认证连接在 20 秒后关闭。
- 重新生成或作废临时密码只作用于未来认证；已经授权的访问继续运行。需要断开当前所有访问时停止共享/停止 Agent。UI 和 CLI 明确区分这两种操作。
- 普通用户运行的 Agent 提供普通用户终端。只有设备管理员显式使用 root 运行服务时才提供 root 终端。软件不修改系统密码，不写入 `NOPASSWD`，不清空 root 密码。

## 本地凭据与客户端

身份文件夹为 700，文件为 600；拒绝符号链接、非当前用户所有或权限过宽的凭据文件。更新通过跨进程锁、临时文件、fsync 和原子替换完成。证书导出不覆盖已有文件。

`.shunyi-cert` 包含客户端私钥，保管方式应与 SSH 私钥相同。Mac WebView 只接收证书文件路径与公开信息，私钥解析及 TLS 在 Rust 侧执行。临时密码只短暂经过内存和 IPC，不写入主机配置、日志或普通导出文件。复制密码会进入操作系统剪贴板，软件不会替用户分发。

主机配置导出使用字段白名单，不导出任何密码。已有 SSH 密码配置仍保留当前本机保存方式；它不属于临时密码存储，不应当把该文件当作加密密码库。SSH 校验系统 known_hosts，未知或变化的服务器密钥被拒绝。

当前 CA/设备叶证书有效期为生成后 10 年，访问证书轮换不能延长设备身份本身的生命周期。设备证书过期需要重新初始化身份并重新分发设备 ID。丢失 CA 私钥、设备私钥泄露或设备被攻陷同样需要更换设备身份。保持操作系统时钟正确。

## 运行限制

隧道/加密帧上限 64 KiB、分块 16 KiB、队列有界；每客户端 WebSocket 至多 1 个设备通道，每设备至多 32 个通道，每次认证访问至多 16 个 PTY。积压超限会关闭连接，不静默丢弃终端输入。握手有时限，设备访问每 15 秒心跳，45 秒无有效客户端消息关闭。中继 60 秒无消息关闭连接。

内置中继默认监听回环地址。公网部署需要可信 WSS 和网络级限流/连接限制；可选中继令牌只控制中继准入，设备权限仍由证书或临时密码决定。当前没有账号体系、P2P、审计录像、任务断线保活、root 自动提权或远程桌面。

本版本经过仓库内测试和本机验收，不代表独立安全审计或公网压力验证已完成。不要在公开 issue 中粘贴私钥、临时密码或完整生产配置。本仓库已启用 GitHub 私密漏洞报告。发现漏洞时，请通过 [私密报告入口](https://github.com/sunguosheng0823-maker/Shunyi/security/advisories/new) 提交复现步骤、受影响版本与脱敏说明；不要创建包含利用细节或生产凭据的公开 issue。


## 支持范围

当前维护版本为 0.1.0 预发布版。旧版未认证协议不再受支持，请将客户端、Agent 和中继配套更新。维护者会根据报告评估影响与修复优先级；当前没有承诺固定响应时限。
